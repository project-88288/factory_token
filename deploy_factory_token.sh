terrain deploy factory_token --signer test --config-path ./config.terrain.json
# when deploy factory_token 
# To determine which <token_contract_code_id>, 
# check the file refs.terrain.json 
# from the workspace's root under the cw20_factory_token object.