terrain deploy cw20_factory_token --signer test --config-path ./config.terrain_cw20.json
# when deploy cw20_factory_token first then 
# get <token_contract_code_id> from refs.terrain.json to determine which <token_contract_code_id> for token_factory